import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'plus.unsplash.com','images.pexels.com', 'fastly.picsum.photos', 'picsum.photos', 'examples.motion.dev', 'framerusercontent.com'],
  },
};

export default nextConfig;
